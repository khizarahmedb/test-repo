/** @type {import('next').NextConfig} */
const nextConfig = {
  // ... other configurations ...
  images: {
    domains: ["files.tfgsolutions.pk", "placehold.co"],
  },
};

export default nextConfig;
